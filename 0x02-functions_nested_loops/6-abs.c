#include "main.h"
/**
 * _abs - Compute absolute value
 * @n: Integer to compute abs value
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
