#include <stdlib>
/*
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
	char abc

	for (abc = 'a'; abc <= 'z'; abc++)
	{
	if ((abc != 'e') (abc != 'e'))
	putchar(abc);
	}
	putchar('\n')
	return (0);
}
